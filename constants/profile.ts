export const PROFILE_FORM_ERRORS = {
  invalidData: "profileInvalidData",
} as const;

export const PROFILE_NAME_MAX_LENGTH = 120;

export const WITHDRAWAL_REQUEST_FIELDS = {
  purchaseId: "purchaseId",
  requesterName: "requesterName",
  confirmed: "withdrawalConfirmed",
} as const;
